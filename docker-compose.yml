services:
  node-app:
    image: artemzatula/node-app-amd64
    build: .
    ports:
      - "3000:3000"
    env_file:
      - ./env/.env
    depends_on:
      - mongo
    command: npm run start

  mongo:
    image: mongo

  redis:
    image: redis