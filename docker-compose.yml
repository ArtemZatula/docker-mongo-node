services:
  node-app:
    image: artemzatula/node-app-amd64
    build: .
    env_file:
      - ./env/.env
    depends_on:
      - mongo
      - redis
    command: npm run start

  mongo:
    image: mongo
    volumes:
      - mongo-db:/data/db

  redis:
    image: redis