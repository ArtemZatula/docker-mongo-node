services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: production
    ports:
      - "80:80"
    env_file:
      - ./env/.prod
    volumes:
      - ./:/app
    command: node index.js

volumes:
  mongo-db:
    driver: local
