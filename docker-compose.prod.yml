services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: production
    ports:
      - "3000:3000"
    env_file:
      - ./env/.prod
    volumes:
      - ./:/app
    command: node index.js

volumes:
  mongo-db:
    driver: local
