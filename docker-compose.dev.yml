services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: development
    ports:
      - "3000:3000"
    env_file:
      - ./env/.dev
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev

volumes:
  mongo-db:
    driver: local

